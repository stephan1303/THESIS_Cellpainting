python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt


---

## 3) requirements.txt updaten

Je huidige requirements zijn best minimaal :contentReference[oaicite:2]{index=2}.  
Voor jouw scripts (convert + cv2 + tifffile + timm) is dit praktischer.

Vervang jouw `requirements.txt` door dit:

```txt
torch==2.2.*
torchvision==0.17.*

numpy>=1.24,<2
pandas>=2.0,<3
scikit-learn>=1.3,<2

opencv-python>=4.8,<5
tifffile>=2023.0.0

Pillow>=10,<11
matplotlib>=3.7,<3.9
tqdm>=4.66

timm>=0.9.12
